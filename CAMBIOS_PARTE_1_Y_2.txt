LOGIROUTE - CORRECCIONES PARTE 1 Y 2
====================================

PARTE 1 - SEGURIDAD
- Eliminados los archivos sshkey y sshkey.pub del proyecto.
- Ampliado .gitignore para impedir que se suban claves privadas, archivos .env y configuraciones de producción sensibles.
- Importante: si la clave eliminada fue usada en GitHub u otro servidor, debe revocarse y reemplazarse por una nueva.

PARTE 2 - ERRORES FUNCIONALES
- Creada la vista faltante templates/cliente/pedido-detalle.html.
- La vista valida visualmente estado, costo, repartidor y fechas del pedido.
- Agregados latitude y longitude a Pedido, PedidoDTO y PedidoResponseDTO.
- Actualizados PedidoMapper y PedidoService para conservar las coordenadas.
- Agregadas columnas latitude y longitude a la tabla pedidos de schema.sql.
- El mapa del repartidor ya no falla por propiedades inexistentes y muestra un aviso cuando el pedido todavía no posee coordenadas.

PRUEBA RECOMENDADA
1. Abrir como cliente: /cliente/pedidos
2. Pulsar "Ver detalle" y comprobar que la vista carga.
3. Abrir como repartidor: /repartidor/mapa
4. Comprobar que la página carga; pedidos sin coordenadas muestran el aviso correspondiente.

NOTA
No se pudo ejecutar Maven en el entorno de revisión porque el wrapper requiere descargar Maven desde Internet. Los cambios fueron verificados por estructura, referencias y consistencia de nombres.
